apiVersion: v1
kind: Service
metadata:
  name: {{ include "hades-artemis-adapter.fullname" . }}
  labels:
    {{- include "hades-artemis-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hades-artemis-adapter.selectorLabels" . | nindent 4 }}
