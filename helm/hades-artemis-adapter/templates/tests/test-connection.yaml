apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hades-artemis-adapter.fullname" . }}-test-connection"
  labels:
    {{- include "hades-artemis-adapter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hades-artemis-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
